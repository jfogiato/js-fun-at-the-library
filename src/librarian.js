class Librarian {
  constructor(name, library) {
    this.name = name
    this.library = library
  }
  greetPatron(name, bool) {
    return (bool ?  `Good morning, ${name}!` : `Hello, ${name}!`)
  }
  findBook(title) {
   
    for (i = 0; i < library.shelves[which shelf to check].length; i++) {
\
    
    // return (library.shelves ? `Yes, we have ${title}`: `Sorry we do not have ${title}`)
  }
}

module.exports = Librarian;